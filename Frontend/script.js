// Inicializa la onda de sonido
const siriWave = new SiriWave({
    container: document.getElementById('siri-container'),
    width: 100,
    height: 50,
    speed: 0.03,
    amplitude: 0.5,
    frequency: 2,
    color: '#00AAFF',
    autostart: true
  });